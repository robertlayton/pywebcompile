#summary A module containing defaults for creating Websites

= Introduction =

WebsiteDefault is a module that contains default values for a variety of properties usually used for setting up websites. If a Website instances doesn't have a property explicitly set, it will use the default found in this module.